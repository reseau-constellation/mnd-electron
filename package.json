{
  "name": "root",
  "version": "0.3.1",
  "license": "AGPL-3.0-or-later",
  "private": false,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "compiler": "pnpm lerna run compiler",
    "formatter": "pnpm lerna run formatter",
    "test": "pnpm compiler && pnpm vitest run",
    "publier": "pnpm lerna publish",
    "versionner": "pnpm lerna version"
  },
  "devDependencies": {
    "@constl/utils-tests": "^1.2.5",
    "lerna": "^8.1.2",
    "playwright": "^1.43.1",
    "vitest": "^1.5.0"
  }
}
