{
  "name": "root",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "compiler": "pnpm lerna run compiler",
    "tester": "pnpm lerna run tester",
    "publier": "pnpm lerna run publier"
  },
  "devDependencies": {
    "lerna": "^6.1.0"
  }
}
