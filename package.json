{
  "name": "root",
  "version": "1.0.0",
  "license": "AGPL-3.0-or-later",
  "private": false,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "compiler": "pnpm lerna run compiler",
    "formatter": "pnpm lerna run formatter",
    "publier": "pnpm lerna run formatter && pnpm test && pnpm lerna publish  --no-private",
    "test": "pnpm compiler && pnpm lerna run test",
    "versionner": "pnpm lerna version"
  },
  "devDependencies": {
    "lerna": "^8.1.3",
    "playwright": "^1.44.1",
    "vitest": "^2.0.0"
  },
  "pnpm": {
    "overrides": {
      "node-datachannel": "^0.11.0"
    }
  }
}
